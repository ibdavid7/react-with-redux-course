import React from 'react';
import {CarForm, CarList} from "../Components";

const CarWidget = () => {
    return (
        <div>
            <CarForm/>
            <CarList/>
        </div>
    );
};

export default CarWidget;
